# Domain-level redirects (www to non-www, http to https) should be configured
# in Cloudflare Dashboard under "Rules" > "Redirect Rules", not here.
# Cloudflare Workers _redirects only supports relative path redirects.
